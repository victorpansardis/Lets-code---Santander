import React from 'react';
import ReactDOM from 'react-dom';
import AppTP from './components/AppTP';
import Hello from './components/Hello';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <AppTP>

    </AppTP>
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
