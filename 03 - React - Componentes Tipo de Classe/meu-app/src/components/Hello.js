import React from 'react';

/*export default function Hello({children}){
    return (
        <>
            {children}
        </>
    );
}*/

/*const Hello = ({text}) => (<h1>{text}</h1>);
export default Hello;*/

const Helllo = ({text}) => <h1>{text}</h1>
export default Helllo;